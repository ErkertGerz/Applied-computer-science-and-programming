#include <iostream>

using namespace std;

int main() {
	setlocale(LC_ALL, "RU");


	cout << "Задание №1" << endl;
	cout << "Данная прогамма проверяет, принадлежит ли задаваемая точка (предположительно) известной вам области." << endl;
	cout << "" << endl;

	float x, y;
	cout << "Введите значение 'x': "; cin >> x;
	cout << "Введите значение 'y': "; cin >> y;

	float k, r;
	k = 3;
	r = 1;

	if (((x >= -1) and (x <= 1) and (y >= -2)) and (y <= 0) or
		(((((x * x) / (k * k)) + ((y * y) / (r * r))) <= 1) and (y >=0)))
		cout << "Точка принадлежит этой области." << endl;
	else cout << "Точка не пренадлежит этой области." << endl;



	return 0;
}
